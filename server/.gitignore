node_modules/
*.log
# Food DB build inputs (large raw dumps — not committed). foods.db is too large to track;
# core-foods.json (small offline core the app ships) IS tracked via the exception below.
data/*.json
!data/core-foods.json
data/*.zip
data/*.db-wal
data/*.db-shm
data/foods.db
data/foods.db.bulk
data/foods.db.*-bak
data/*.bak
