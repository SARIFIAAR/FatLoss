# Fat Loss Coach — project guide for Claude

Native iOS port of a single-file web app (`my_fitness_coach_4.html`, localStorage) for one user's
fat-loss programme. SwiftUI, iOS 17+, iPhone only. This file is the knowledge base: read it before
touching the project.

## Identifiers (do not guess these)

| Thing | Value |
|---|---|
| Bundle ID | `com.MyFatLossCoach.app` |
| App Store Connect app | `6809046427` ("Fat Loss Coach", SKU `FatLoss001`) |
| Apple team | `9F2G8CQ45J` (Infinion Apps FZ-LLC) — **not** the 685W4R3BWT METATEC team used by other repos |
| ASC API key | key ID `F32V65ACX6`, issuer `60384b84-0407-4489-bb63-c2c1b708e376`, file `~/.appstoreconnect/private_keys/AuthKey_F32V65ACX6.p8` |
| Firebase project | `fat-loss-6516d`; iOS app `1:836772213794:ios:91156d2fa72e12d2a6867b` |
| Firebase plan | **Spark (free)** — the owner's Google Payments profile is blocked (OR_BAOOC_15), so nothing may depend on Blaze/Cloud Functions |
| Meal analyzer | Fly.io app `fatloss-analyzer`, region `fra`, `https://fatloss-analyzer.fly.dev/analyze` |
| Repo | `https://github.com/SARIFIAAR/FatLoss` (public) |
| URL scheme | `fatlosscoach://sync?hrv=&rhr=&sleep=&deep=&rem=&resp=&mood=&steps=` |

`FatLossCoach/GoogleService-Info.plist` is git-ignored because the repo is public. Restore it with
`firebase apps:sdkconfig IOS 1:836772213794:ios:91156d2fa72e12d2a6867b --project fat-loss-6516d --out FatLossCoach/GoogleService-Info.plist`.
The app runs without it (cloud sync just reports "not configured").

## Layout

```
FatLossCoach.xcodeproj   folder-synced project (objectVersion 77) — no per-file entries; adding a
                         .swift file under FatLossCoach/ is enough
Info.plist               explicit plist (GENERATE_INFOPLIST_FILE = NO); usage strings + URL scheme
FatLossCoach/
  App/FatLossCoachApp.swift     @main, ContentView (TabView), toast overlay, debug launch args
  Models/Constants.swift        static programme content: Plan.habits/supplements/phases/meals/breathing
  Models/AppData.swift          everything persisted (Codable) + merge logic for cloud sync + DateKey
  Core/Store.swift              single @Observable source of truth; all mutations go through it
  Core/HealthKitManager.swift   steps, resting HR, HRV, respiratory rate, sleep stages (30-day backfill)
  Core/CloudSync.swift          Sign in with Apple → Firestore users/{uid} (whole AppData as one JSON field + profile)
  Core/CloudMirror.swift        flat per-day / per-meal Firestore rows (users/{uid}/days, /meals) for backend trends
  Core/MealScanner.swift        photo → Fly.io analyzer → MealEntry (sends the user's targets as `context`)
  Core/ReminderManager.swift    local notifications: water (interval + hours, "Log 250/500 ml" actions) and walk nudges
  Core/LegacyImport.swift       imports JSON.stringify(localStorage) from the old web app
  DesignSystem/Theme.swift      palette (adaptive light/dark), Card/Screen/SectionTitle, buttons, NumField
  Features/{Today,Progress,Workout,Nutrition,Profile}/   one file per tab
  Assets.xcassets               AppIcon (generated by scripts/make_icon.swift), AccentColor, LaunchBackground
server/                  Node 22 analyzer + admin dashboard for Fly.io (server.js, firestore.js, admin.html, Dockerfile, fly.toml)
scripts/make_icon.swift  regenerates the 1024 px icon
firestore.rules          users/{uid} owner-only; everything else denied
build/                   git-ignored: DerivedData, xcarchive, export/, logs
scripts/export_upload.sh export IPA from build/FatLossCoach.xcarchive + altool upload (run by the user)
scripts/asc_builds.mjs   lists TestFlight builds + processing state via the ASC API (node, no deps)
```

## Data model (mirrors the old localStorage keys)

| Web key | `AppData` field |
|---|---|
| `weight-logs` / `cur-weight` | `weightLogs: [MeasurementEntry]` (one per date, last write wins) |
| `waist-logs` / `cur-waist` | `waistLogs` |
| `rec-YYYY-MM-DD` | `recovery[date]: RecoveryDay` (hrv, rhr, sleepH, deepH, remH, resp, mood) |
| `po-{Exercise}` | `overload[name]: [ExerciseLog]` (date, kg, reps) |
| `habits-YYYY-MM-DD` | `habits[date]: Set<String>` |
| `supps-YYYY-MM-DD` | `supplements[date]: Set<String>` |
| `water-YYYY-MM-DD` | `water[date]: Int` (ml) |
| `health-sync` | `health[date]: HealthDay` (steps, restingHR, activeKcal, basalKcal → `burnedKcal`) |
| `ex-YYYY-MM-DD-Mon` | `exerciseDone["date-Mon"]: Set<Int>` |
| — (new) | `meals[date]: [MealEntry]` (slot = breakfast/lunch/snack/dinner/evening), `breathing[date]`, `goals`, `program` (phase + startDate), `reminders` (ReminderSettings) |

Dates are **local** `yyyy-MM-dd` (`DateKey`). The web app used UTC; imported dates are taken as-is.
Every struct has a lenient `init(from:)` (missing keys → defaults) so old JSON always decodes.
Storage: one JSON file `Application Support/fatloss-data.json`, saved 400 ms after any change and on
background. `AppData.merged(with:)` is a union merge used by cloud sync; scalar conflicts go to the
newer `updatedAt`, programme phase goes to the further-along one.

## Rules that were learned the hard way

- **Never mutate `Store.data` inside its own `didSet`.** Build 1 did (`data.updatedAt = Date()`),
  which re-entered the setter forever and crashed on every write. Modification time lives in
  `Store.lastModified` and is stamped into `snapshot()` only when saving/exporting/pushing.
- `@Observable` classes cannot have `lazy var`; use a computed property (see `MealScanner`).
- Firebase must be configured before anything touches `Functions`/`Firestore`; `@State` property
  initializers run before `App.init`, so keep Firebase access lazy/computed.
- Row views that contain a tappable circle must not nest two Buttons: use `CheckMark` (visual) inside
  the row Button, `CheckCircle` (interactive) only when it stands alone.
- Fill-mode `AsyncImage`s overflow their frame; use `Color.clear.frame(...).overlay { image }.clipShape`.
- The web app's `Plan.workouts` was replaced by `Plan.phases[n].workouts`; use `store.currentPhase`.
- The auto-mode permission classifier blocks `xcodebuild -exportArchive` and `xcrun altool` when Claude
  runs them. Archive is fine; the user runs `sh scripts/export_upload.sh` for export + upload.
- The Mac's disk fills up fast (hit 99 % twice). If a build fails with "No space left on device",
  clear `~/Library/Developer/Xcode/DerivedData` (safe) and say so.
- `~/.npm` is not writable: `npm install --cache <scratch dir>`.
- The user prefers action over questions. Build it, verify it in the simulator, then report.

## Workflows

**Compile check (no permission needed)**
```
xcodebuild -project FatLossCoach.xcodeproj -scheme FatLossCoach \
  -destination 'platform=iOS Simulator,id=52083031-7076-4D40-8776-0CB215E74108' \
  -derivedDataPath build/DerivedData build 2>&1 | grep -E "error:|\*\* BUILD"
```
Simulator: iPhone 17 Pro, iOS 26.2 (`52083031-7076-4D40-8776-0CB215E74108`). If CoreSimulator misbehaves:
`xcrun simctl shutdown all; killall -9 com.apple.CoreSimulator.CoreSimulatorService`.

**Verify UI without tapping** — debug launch arguments:
`-startTab 0..4` (Today, Progress, Workout, Nutrition, Profile), `-openDay Mon` (opens that session
sheet), `-debugLogWeight 101.5` (performs a write on launch; the original crash repro). Seed data by
writing `fatloss-data.json` into the simulator container
(`xcrun simctl get_app_container <sim> com.MyFatLossCoach.app data`), then relaunch and
`xcrun simctl io <sim> screenshot`.

**Ship to TestFlight** (ask the user before uploading; bump `CURRENT_PROJECT_VERSION` in both configs first)
```
xcodebuild -project FatLossCoach.xcodeproj -scheme FatLossCoach -configuration Release \
  -destination 'generic/platform=iOS' -archivePath build/FatLossCoach.xcarchive \
  -allowProvisioningUpdates -authenticationKeyPath ~/.appstoreconnect/private_keys/AuthKey_F32V65ACX6.p8 \
  -authenticationKeyID F32V65ACX6 -authenticationKeyIssuerID 60384b84-0407-4489-bb63-c2c1b708e376 archive
```
then the user runs `! sh ~/Developer/FatLossCoach/scripts/export_upload.sh` (export + altool upload).
Check processing with `node scripts/asc_builds.mjs` (ASC API, `/v1/builds?filter[app]=6809046427`).

**Analyzer + admin dashboard (Fly.io)** — from `server/`: `flyctl deploy --ha=false` (Fly account
firas.raai@gmail.com, personal org, app `fatloss-analyzer`, one shared-cpu machine in `fra` that
auto-stops). Secrets:
- `ANTHROPIC_API_KEY` (required). `MODEL` (optional, default `claude-opus-5`; `claude-sonnet-5` is cheaper).
- `ADMIN_KEY` — long random string; unlocks `https://fatloss-analyzer.fly.dev/admin` (header `x-admin-key`).
- `FIREBASE_SERVICE_ACCOUNT` — the JSON of a service-account key (Firebase console → Project settings →
  Service accounts → Generate new private key; works on Spark). Set with
  `flyctl secrets set FIREBASE_SERVICE_ACCOUNT="$(cat key.json)"`. Enables (a) server-side logging of every
  scan to Firestore `scans/` and (b) the dashboard reading `users/*` rows. `firestore.js` is a tiny REST
  client (jose-signed OAuth) — no firebase-admin, keeps the image small.
`GET /health` reports `{model, firestore, admin}`. `/analyze` needs `Authorization: Bearer <Firebase ID token>`
(verified against Google's `securetoken` JWKS); the request `context` (kcal/protein targets, weight, goal,
slot) feeds the dietitian prompt so it is no longer hard-coded to one person. The Fly secrets command is
blocked for Claude by the permission classifier — the user runs it.

**Backend data model (schema 2, written by the app when signed in)**
- `users/{uid}`: `json` (sync blob) + `profile` {displayName, email, appVersion, build, device, os, timezone,
  createdAt, lastSeenAt} + `goals` + `program` + `stats` {latestWeight, kgLost, dayCount, mealCount, …} + `schema`.
- `users/{uid}/days/{yyyy-MM-dd}`: one wide row per day — weightKg, waistCm, hrv, rhr, sleepH, deepH, remH,
  resp, mood, readiness, steps, restingHR, activeKcal, basalKcal, burnedKcal, waterMl, habits[], habitCount,
  supplements[], breathing[], mealCount, kcal, protein, carbs, fat, deficit (burned − eaten), lifts{ex:{kg,reps,volume}},
  exercisesDone, updatedAt. `CloudMirror` diffs each row's SHA-256 against a per-uid cache in UserDefaults and
  only writes what changed; deleted meals delete their row. Schema-1 accounts back-fill on first sync.
- `users/{uid}/meals/{mealId}`: date, at, slot, name, kcal/protein/carbs/fat, items[], confidence, notes.
- `scans/{auto}` (server only): uid, email, at, ms, model, ok, isFood, name, kcal…, tokensIn/Out, error.
- Rules: owner-only on `users/{uid}` and its `days`/`meals`; `scans` and everything else denied to clients.
- Each person who signs in with Apple gets their own `users/{uid}` tree automatically — that is the
  "separate profile per user"; there is no admin role in the app itself, only the web dashboard.

**Firestore rules**: `firebase deploy --only firestore:rules --project fat-loss-6516d`.

## Feature status (build 7)

- Today: phase strip, eaten/target macros, Apple Watch steps + kcal burned (active + resting) + RHR with a
  burned/eaten/deficit line, readiness score (HRV 40 · sleep 30
  +5 deep +5 REM · RHR 20), habits (7; supplements/breathing/water auto-tick), water, supplements,
  tickable breathing, weight/waist logging.
- Progress: weight goal bar, waist + weight charts (30 d, goal line), daily calories stacked by
  protein/carbs/fat, energy balance 7 d (eaten vs burned bars + per-day deficit labels + average), weekly steps, HRV 30 nights, sleep 14 nights, 28-day habit heat-map, supplement adherence.
- Workout: 3-phase programme (Foundation 4 wk → Build 4 wk → Full Gym 8 wk) with a progress bar, week
  counter, start/advance/change controls; per-phase schedule with exercise photos
  (raw.githubusercontent.com/yuhonas/free-exercise-db) and progressive-overload logging.
- Nutrition: meal scanner (camera/library, per-meal-slot buttons), eaten-today list, water tracker,
  meal plan with eaten vs planned kcal, Watch burned / deficit-so-far + 7-day average deficit line,
  9 PM kitchen-closed banner.
- Profile: goals editor, cloud sync (Sign in with Apple), Apple Health connect/sync + manual entry,
  water/walk reminders,
  Shortcuts URL template, import from web app, JSON export, version/build footer.

## Where things stand (2026-09-07)

**Done today (uncommitted → committed, NOT yet built for TestFlight, server NOT yet redeployed).**
- The Anthropic key on Fly went invalid (`authentication_error: API key is invalid` → app showed
  "Model error 401"); a new key was set with `flyctl secrets set ANTHROPIC_API_KEY=…` and verified.
- **Three ways to log every meal**: photo (as before), **Type it in** (search the USDA FoodData Central
  database, pick a household portion or grams × quantity, build the meal from several foods) and
  **Barcode** (Open Food Facts first, USDA Branded fallback). Plus **AI text estimate** as a fallback
  from the search sheet ("Ask AI to estimate …" → `/analyze` with `text` instead of `image`).
  Files: `Core/FoodSearch.swift` (client for `/foods` + `/barcode`), `Features/Nutrition/FoodEntrySheet.swift`
  (search / basket / `PortionSheet`), `Features/Nutrition/BarcodeScannerView.swift` (VisionKit
  `DataScannerViewController`, real device only), `MealScanner.analyze(text:)`, `ScanFlow.typed(slot:barcode:)`,
  `MealResultSheet.image` is optional. Meal-plan rows now show a "+" menu: Take photo / Choose from
  library / Type it in / Scan barcode.
- Server: `GET /foods?q=` (USDA search, cached 6 h, ranked: all query words → fewest extra words →
  USDA order; generic before branded), `GET /barcode?code=`, `/analyze` accepts `{ text }`.
  New optional secret **`USDA_API_KEY`** (defaults to `DEMO_KEY`, 30 requests/hour per IP — get a free
  key at https://fdc.nal.usda.gov/api-key-signup and `flyctl -a fatloss-analyzer secrets set USDA_API_KEY=…`).
- **Guided breathing** (ported from the user's Flutter "Breathing Exercise" app in ~/Downloads): tapping a row
  in the Today breathing schedule opens `Features/Today/BreathingSessionView.swift` — intro with per-technique
  guide text, 3-s countdown, circle grows on inhale / shrinks on exhale, phase ring + seconds, spoken cues via
  `AVSpeechSynthesizer` (toggle, persisted `breathVoice`), haptics per phase, pause/end, finishing calls
  `Store.markBreathingDone`. `BreathingSlot` gained `pattern`/`minutes`/`cycles`/`guide`. Debug launch args:
  `-breathing "Box Breathing"` opens a session, `-breathingStart 1` auto-starts it.
- Server redeployed and `USDA_API_KEY` set by the user on 2026-09-07 (`/health` → `"foods":"usda"`).
- **To ship:** bump to build 8
  and run `build/export_upload.sh` (user runs with `!`).

## Where things stood (2026-09-06, night)

**Shipped.** Build 1.0.0 (7) uploaded to TestFlight 2026-09-06 (delivery UUID
`c805a13d-0cb2-4f4e-8f99-b32be85d89c8`) and **processed VALID** (`node scripts/asc_builds.mjs`). It contains:
Apple Watch energy + deficit (Today / Nutrition / Progress `EnergyChartView`), `CloudMirror` per-user rows +
richer profile doc (schema 2), `MealScanner.Context`, and water/walk reminders. Existing users get a one-time
Health permission sheet for the two energy types. Build 6 is still valid on TestFlight. `main` is pushed and
clean (`a0187a8` features, `4c37f14` bump). The analyzer is live at `https://fatloss-analyzer.fly.dev`
(`/health` OK; the user's banana scan worked from build 6).

**Waiting on the user (server secrets, Claude cannot set them):**
```
flyctl -a fatloss-analyzer secrets set ADMIN_KEY="$(openssl rand -hex 24)"
flyctl -a fatloss-analyzer secrets set FIREBASE_SERVICE_ACCOUNT="$(cat ~/Downloads/fat-loss-6516d-*.json)"
```
Checked 2026-09-06 night: `/health` still returns `{"firestore":false,"admin":false}`, so `/admin` says the
key/service account is not configured and scans are not logged server-side.
The Anthropic key was pasted into the chat transcript twice (09-06 and 09-07); the user should rotate it.

**Housekeeping still open.**
- A stray Firebase iOS app (bundle `com.metatec.myfitnesscoach`) still exists in project
  `fat-loss-6516d`; delete it in the console.
- The user is on Phase 1 and has not pressed "Start programme" yet; the phase strip shows week 0.
- `build/archive*.log` and `build/build*.log` are git-ignored scratch; safe to delete when disk is tight.
- README "Status" and "Build history" and this section must be updated together whenever a build ships.

**Likely next work.** Set the two Fly secrets and check the dashboard; invite testers (each signs in with
Apple in Profile → own profile); a personal-baseline stress score (HRV/RHR/resp/sleep z-scores vs a 28-day
median, discussed 2026-09-06, not built); Sonnet vs Opus cost decision (`MODEL` secret).
